{
  "name": "leagueoffline",
  "version": "1.0.0",
  "description": "League Offline is a tool to vanish from your friendslist while playing games.",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "package-win": "electron-packager . LeagueOffline --overwrite --asar=true --platform=win32 --arch=ia32 --prune=true --out=release-builds --version-string.CompanyName=CE --version-string.FileDescription=CE --version-string.ProductName=\"League Offline\""
  },
  "author": "Juri Adams <juri@4da.ms>",
  "license": "MIT",
  "dependencies": {
    "electron": "^1.7.9",
    "electron-open-link-in-browser": "^1.0.2",
    "node-cmd": "^3.0.0"
  },
  "devDependencies": {
    "electron-packager": "^10.1.0"
  }
}
